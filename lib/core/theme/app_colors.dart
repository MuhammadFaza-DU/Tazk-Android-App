import 'package:flutter/material.dart';

/// Palet "Warna Alam" — hijau daun, kuning keemasan, terracotta.
class AppColors {
  AppColors._();

  // Primary — hijau daun
  static const Color primaryLight = Color(0xFF2F6B3D);
  static const Color primaryDark = Color(0xFF7ED08A);

  // Secondary/Accent — kuning keemasan / oranye hangat
  static const Color accentLight = Color(0xFFF0A83C);
  static const Color accentDark = Color(0xFFFFC069);

  // Supporting — coklat tanah / terracotta
  static const Color terracotta = Color(0xFFB0662E);

  // Backgrounds
  static const Color backgroundLight = Color(0xFFFAF7F0);
  static const Color backgroundDark = Color(0xFF16231A);
  static const Color surfaceLight = Color(0xFFFFFFFF);
  static const Color surfaceDark = Color(0xFF1E2E22);
}
